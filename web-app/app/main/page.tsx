import MainPage from "@/components/main/MainPage";
import { mainPageData } from "@/components/main/data";

export default function MainRoutePage() {
  // Swap `mainPageData` with your API response once ready.
  return <MainPage data={mainPageData} />;
}
